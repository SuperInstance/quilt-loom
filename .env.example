# quilt-loom API keys — copy to .env and fill, or export directly.
# NEVER commit real keys. Offline legs run with no keys at all.

# typesafe.ai — System One typed decisions
TYPESAFE_API_KEY=

# mothquantum.com — quantum entropy + entanglement (https://docs.mothquantum.com)
MOTH_API_KEY=

# optional GLM lane (z-ai sdk)
ZAI_API_KEY=
